<Window x:Class="CassaSystem.Views.LoginPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CassaSystem.Views"
        mc:Ignorable="d"
        Title="LoginPage" Height="450" Width="440">
    <Grid Background="AliceBlue">
        <!-- Üst tərəfdə başlıq -->
        <TextBlock Text="Login" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Top" 
                   FontSize="28" 
                   FontWeight="Bold" 
                   Margin="0,30,0,0"/>

        <!-- Istifadəçi girişi paneli -->
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="250">
            <!-- Istifadəçi adı -->
            <TextBlock Text="Username" FontSize="14" Margin="0,10,0,0"/>
            <TextBox x:Name="UsernameTextBox" Height="30" Margin="0,5,0,10" />

            <!-- Parol -->
            <TextBlock Text="Password" FontSize="14"/>
            <PasswordBox x:Name="PasswordBox" Height="30" Margin="0,5,0,10" />

            <!-- Login düyməsi -->
            <Button Content="Login" Width="150" Height="40" Margin="0,20,0,0" Click="LoginButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
